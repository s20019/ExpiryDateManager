<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.ac.it_college.std.s20019.expirydatemanager2">

    <application
        android:name=".ExpiryDateManagerApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_calendar"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_calendar_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpiryDateManager2">
        <activity
            android:name=".activity.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ExpiryDateManager2.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>